# 의존성 라이브러리 (용량이 크고 패키지 매니저로 설치 가능하므로 제외)
node_modules/
jspm_packages/

# 빌드 결과물
dist/
build/
out/

# 환경 변수 및 비밀 키 파일 (★절대 깃허브에 올라가면 안 되는 파일)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 운영체제 및 IDE 개인 설정 파일
.DS_Store
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp